## let's create a truncated version of the inverse-transform method


## 1. generate U ~ U(0, 1)
## 2. return Z = F-1(F(a-) + U(F(b) - F(a-))).

# Generate a random uniform vector

m <- 1000

x <- rnorm()